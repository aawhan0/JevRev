# Benchmark methodology

All routing strategies will be evaluated on the same query set.

Metrics:
- accuracy / task quality
- latency
- estimated cost
- large-model calls avoided
- fallback rate

The current dataset is a development fixture, not a statistically meaningful result. Before publishing findings, expand the dataset, pin model versions, document pricing assumptions, and repeat trials.
